# --- secrets: never commit signing keys -------------------------------------
cosign.key
*.key
*.pem

# --- build outputs ----------------------------------------------------------
output/
*.iso
*.qcow2
*.raw
*.img
*.tar
*.oci

# --- language tooling -------------------------------------------------------
__pycache__/
*.pyc
.venv/
.ruff_cache/
node_modules/
dist/
target/

# --- editors / OS -----------------------------------------------------------
.DS_Store
*.swp
.idea/
.vscode/
# Qt Creator per-user project files and qmlls state
*.user
*.user.*
*.autosave
.qmlls.ini

# --- brand guide canvas ------------------------------------------------------
# `_ds/` is an unrelated "Organic" design system (terracotta/sage, Caprasimo)
# auto-generated by the design tool. It is not referenced by the canvas and
# contradicts brand/tokens.css. Keep it out of the repo.
docs/brand-guide/_ds/

# --- C++ / CMake / clangd --------------------------------------------------
build/
build-*/
CMakeUserPresets.json
compile_commands.json
.cache/
