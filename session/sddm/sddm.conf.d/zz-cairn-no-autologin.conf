# SPDX-License-Identifier: Apache-2.0
# Nobody logs in without the greeter (ADR-0016). Plasma's first-run wizard
# leaves a 99-plasma-setup.conf behind that autologs its own setup user
# into a "plasma" session; this file sorts after it and empties both keys.
[Autologin]
User=
Session=
